# Adult Census Income 팀 논의사항과 최종 결정

이 문서는 프로젝트에서 정답이 하나로 정해지지 않은 선택지를 정리하고, 현재 코드에 반영한 팀 기준과 이유를 기록합니다. 구현 결과의 전체 요구사항 대응 여부는 [43개 기준 체크리스트](day2_outputs/criteria_checklist.md)에서 확인할 수 있습니다.

## 최종 결정 요약

| 주제 | 최종 결정 |
| --- | --- |
| 분석 범위 | 데이터 준비 → EDA → 통계 → 모델 → 저장 → 보고서 End-to-End |
| 데이터 | `adult.data` 하나를 80:20으로 분할 |
| 재현성 | `random_state=42`, 타깃 비율을 유지하는 stratified split |
| 결측치 | `?`를 null로 인식, 행 삭제 없이 train 중앙값·최빈값 대체 |
| 중복 | 완전 중복만 분석용 데이터에서 제거, 원본 보존 |
| 이상치·희소 범주 | 실제 가능한 값으로 보고 유지, 진단 수치만 기록 |
| 학력 변수 | EDA는 `education`, 모델은 `education-num` |
| 기본 모델 | `class_weight="balanced"` Logistic Regression |
| 최종 모델 | F1과 설명 가능성을 고려해 `without_fnlwgt` |
| 민감 변수 | 최종 후보에는 포함하고 제외 모델로 안정성 감사, 윤리적 한계 명시 |

## 1. 재현성을 위한 Random seed

### 선택지

- 특정 정수로 고정: 실행 결과를 재현하고 팀원 결과를 비교할 수 있음
- 미지정: 실행마다 분할과 결과가 달라질 수 있어 팀 프로젝트에 부적합

### 최종 결정

`train_test_split`과 `LogisticRegression` 모두 `random_state=42`를 사용합니다. 숫자 42 자체가 성능을 보장하지는 않지만 팀 전체의 재현 기준을 하나로 맞추는 데 의미가 있습니다.

## 2. 수치형 결측치 대체

### 선택지

- 평균: 대칭 분포와 이상치가 적은 변수에 적합
- 중앙값: 왜도와 극단값 영향에 강함
- 행 삭제: 결측이 극히 적을 때만 고려
- KNN·회귀 대체: 다른 변수 관계를 반영하지만 복잡도가 큼

### 최종 결정

수치형은 `SimpleImputer(strategy="median")`을 사용합니다. 현재 원본 수치형 결측은 0건이지만, 향후 실제 입력에 결측이 들어오는 상황을 위한 안전망입니다. `capital-gain`, `capital-loss`, `fnlwgt`처럼 분포가 치우친 변수가 있어 평균보다 중앙값이 안정적입니다.

## 3. 범주형 결측치 대체

### 선택지

- 최빈값: 구현이 단순하고 Pipeline에 자연스럽게 포함됨
- `Unknown` 범주: 결측 자체의 신호를 보존할 수 있음
- 행 삭제: `workclass`와 `occupation` 결측률이 약 5.6%라 데이터 손실이 큼

### 최종 결정

`SimpleImputer(strategy="most_frequent")`을 사용합니다. 단, 대체값은 전체 데이터가 아니라 split 이후 train 데이터에서만 계산합니다. 현재 최종 Pipeline이 train에서 학습한 값은 다음과 같습니다.

| 컬럼 | 최빈값 |
| --- | --- |
| workclass | Private |
| marital-status | Married-civ-spouse |
| occupation | Prof-specialty |
| relationship | Husband |
| race | White |
| sex | Male |
| native-country | United-States |

## 4. 결측치 처리 시점과 데이터 누수

### 선택지

- 전체 데이터를 먼저 대체한 뒤 분할: test 정보가 전처리 기준에 섞이는 데이터 누수 발생
- 먼저 분할하고 train으로만 대체기 학습: 평가 신뢰성을 유지

### 최종 결정

80:20으로 먼저 분할하고, 결측 대체·스케일링·원-핫 인코딩을 포함한 Pipeline을 train에만 `fit`합니다. test에는 학습된 기준으로 `transform`만 적용합니다. EDA에서는 결측 상태 자체를 보여주기 위해 전역 대체를 하지 않습니다.

## 5. train/test 분할 비율

### 선택지

- 70:30: 평가 표본을 더 많이 확보
- 80:20: 수만 건 데이터에서 학습량과 평가량의 균형
- 공식 `adult.test`: 별도 파일과 라벨 형식 정리가 필요

### 최종 결정

`test_size=0.2`, `random_state=42`, `stratify=y`를 사용합니다. 중복 제거 후 train 26,029행, test 6,508행이며 test의 `>50K`도 1,568건이어서 평가에 충분합니다.

## 6. 클래스 불균형 대응

### 선택지

- 아무 처리 없음: 다수 클래스 중심으로 Accuracy가 높아질 수 있음
- `class_weight="balanced"`: 소수 클래스 오류에 더 큰 비용 부여
- SMOTE·오버샘플링: 추가 의존성과 검증 절차 필요
- 분류 임계값 조정: 업무 비용에 근거한 임계값 결정 필요

### 최종 결정

수업 범위와 구현 설명 가능성을 고려해 `class_weight="balanced"`를 사용합니다. 그 결과 `>50K` Recall은 0.8603으로 높지만 Precision은 0.5685이므로 두 지표의 trade-off를 함께 설명합니다.

## 7. 이상치 처리

### 대상

`age`, `hours-per-week`, `capital-gain`, `capital-loss`, `fnlwgt`

### 최종 결정

IQR 경계 밖 관측 수는 이상치 후보로 집계하지만 삭제하거나 클리핑하지 않습니다.

- `capital-gain`과 `capital-loss`의 큰 값은 실제 자본 거래일 수 있음
- `fnlwgt`는 각 행이 모집단에서 대표하는 규모를 나타내는 공식 표본 가중치
- `age` 17~90과 `hours-per-week` 1~99는 가능한 관측 범위
- `capital-gain`과 `capital-loss`는 시각화에서만 `log1p`를 사용하고 모델 입력은 유지

## 8. 희소 범주 처리

### 선택지

- 상위 N개 외 `Other` 통합: 그래프는 단순하지만 정보가 사라짐
- 모든 범주 유지: 데이터의 다양성을 보존하지만 일부 비율이 불안정할 수 있음

### 최종 결정

EDA 상세 표와 모델에서는 모든 범주를 유지합니다. 표본 100건 미만 범주는 희소 범주로 표시하고, 고소득 비율 막대그래프에서는 비율의 불안정성을 줄이기 위해 100건 이상 그룹만 보여줍니다. `OneHotEncoder(handle_unknown="ignore")`로 실제 서비스의 새 범주에도 대응합니다.

## 9. education과 education-num

### 선택지

- 둘 다 모델에 포함: 정보 중복과 계수 해석 불안정 가능
- `education`만 포함: 범주별 비선형 차이를 반영하지만 차원이 늘어남
- `education-num`만 포함: 단순하고 학력 순서를 숫자로 반영

### 최종 결정

시각화와 범주 비율 해석에는 `education`을 사용하고 모델에는 `education-num`만 사용합니다. 같은 학력 정보를 두 번 입력하는 것을 피하면서 EDA와 모델링 각각의 장점을 살립니다.

## 10. 상관분석 방법

### 선택지

- Pearson: 선형 관계
- Spearman: 순위 기반 단조 관계, 극단값에 상대적으로 강함
- Kendall: 작은 표본의 순위 관계에 유용

### 최종 결정

수업 기준인 Pearson을 기본으로 계산하고 Spearman을 함께 저장해 왜도가 큰 변수의 민감도를 비교합니다. `income`은 참고용으로만 0·1 매핑하며, 상관은 인과관계가 아님을 보고서에 명시합니다.

## 11. 통계 검정 방법과 유의수준

### 최종 결정

- 모든 평균 비교 가설은 결과 확인 전에 양측으로 정의
- D’Agostino 정규성 검정과 Levene 등분산 검정 결과 기록
- 등분산이면 Student, 아니면 Welch t-test 사용
- 비정규성 민감도 확인을 위해 Mann–Whitney U p-value도 저장
- 주 유의수준은 0.05, 0.01과 0.10 플래그도 함께 제공
- p-value 외에 평균 차이, 95% 신뢰구간, Cohen’s d를 함께 해석

표본이 매우 크면 작은 차이도 통계적으로 유의할 수 있으므로 유의성과 실제 효과 크기를 구분합니다.

## 12. 분류 모델과 전처리

### 선택지

- Logistic Regression: 빠르고 설명이 쉬운 이진 분류 모델
- Random Forest·Gradient Boosting: 비선형 관계를 반영하지만 설명이 복잡

### 최종 결정

기본 모델은 Logistic Regression입니다.

- 수치형: train 중앙값 대체 → `StandardScaler`
- 범주형: train 최빈값 대체 → `OneHotEncoder`
- 분류기: `LogisticRegression(class_weight="balanced")`
- 전체 전처리와 모델을 하나의 sklearn `Pipeline`으로 저장

## 13. fnlwgt 포함 여부와 최종 모델 선택

### 비교 결과

| 모델 | F1 | ROC-AUC | 해석 |
| --- | ---: | ---: | --- |
| without_fnlwgt | 0.6846 | 0.9094 | 개인 특성이 아닌 표본 가중치 제외 |
| all_features | 0.6836 | 0.9096 | fnlwgt와 민감 변수 포함 |
| without_sensitive | 0.6832 | 0.9093 | race·sex 제외 감사 모델 |

### 최종 결정

`all_features`와 F1 차이가 0.005 이내이면 설명이 쉬운 모델을 선택한다는 기준에 따라 `without_fnlwgt`를 최종 모델로 정했습니다. ROC-AUC는 all_features가 0.0002 높지만 F1은 without_fnlwgt가 더 높고, 표본 가중치를 개인 소득 예측 feature로 설명하기 어렵다는 점을 함께 고려했습니다.

## 14. 민감 변수와 공정성

최종 후보 모델에는 `race`와 `sex`를 포함하되, `without_sensitive` 모델로 제외 전후 성능 안정성을 확인합니다. 민감 변수를 제외하는 것만으로 공정성이 보장되지는 않으며 `education`, `occupation` 같은 대리 변수가 남을 수 있습니다. 실제 의사결정에는 사용하지 않고, 활용 전 집단별 오류율과 공정성 지표를 추가 검토해야 합니다.

## 15. Plotly 데이터 크기

- 집계 막대그래프·Treemap은 집계 결과를 사용하므로 전체 데이터를 반영
- 연령·근무시간 산점도는 브라우저 반응성과 대표성의 균형을 위해 6,000행 표본 사용
- Parallel Categories는 5,000행 표본 사용
- 모든 표본 추출은 `random_state=42`로 재현 가능

## 발표에서 강조할 사항

1. 전처리 기준을 train에서만 학습해 데이터 누수를 막았습니다.
2. 극단값과 희소 범주를 무조건 제거하지 않고 변수의 의미를 먼저 고려했습니다.
3. p-value만 제시하지 않고 효과 크기와 신뢰구간을 함께 해석했습니다.
4. Accuracy 외에 Precision, Recall, F1, ROC-AUC와 Confusion Matrix를 모두 확인했습니다.
5. 성능 차이가 작을 때는 F1뿐 아니라 feature 의미와 설명 가능성을 모델 선택에 반영했습니다.
