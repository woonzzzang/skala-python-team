# Adult Census Income 분석 프로젝트 의존 패키지
# 교육용 프로젝트라 정확한 버전 고정(==) 대신 최소 버전(>=)만 지정한다.
# 이유: 학생마다 파이썬/운영체제가 다를 수 있어 지나치게 좁은 버전 고정은
# 설치 실패를 일으키기 쉽고, 이 프로젝트는 재현성보다 실습 목적이 우선이다.
# (배포용 프로젝트라면 pip freeze로 정확한 버전을 고정하는 것이 좋다.)

pandas>=2.0
polars>=1.0
numpy>=1.26
scipy>=1.11
matplotlib>=3.8
seaborn>=0.13
plotly>=5.20
scikit-learn>=1.4
joblib>=1.3
tabulate>=0.9

# 선택 사항: src/main.py의 schedule_daily_run()을 실제로 쓰고 싶을 때만 설치한다.
# 기본 실행(python src/main.py)에는 필요 없다.
# schedule>=1.2
